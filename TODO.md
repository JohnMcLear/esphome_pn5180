# PN5180 Component TODOs

## Features to Implement
- [ ] Health Check Logic: Re-add support for the following configuration options in `ci-test.yaml` and implement the corresponding logic in the C++ component:
    - `health_check_enabled`
    - `health_check_interval`
    - `max_failed_checks`
    - `auto_reset_on_failure`
- [ ] Support for ISO14443B (Type B) cards
- [ ] Support for ISO15693 cards (ICODE SLIX, etc.)
- [ ] Support for FeliCa cards
- [ ] Card emulation mode (message support)
